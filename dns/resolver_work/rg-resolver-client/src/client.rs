fn main() {
    // Connect to the resolver (TCP)
    // Create the request (JSON-RPC)
    //     * NOTE: No 'id' field needed b/c we're using a TCP connection for each request.
    // Send the request to the resolver
    // Wait for a response
    // Process the response
    let query = rg_resolver::
}